# Variables du provisioning Phase 0 (Caddy + DuckDNS).
# Copier en deploy/.env sur le serveur (jamais commité) et compléter.

# Domaine(s) servi(s) par Caddy (S-03). Plusieurs domaines : séparés par ", " (site address list Caddy).
LUTECIUM_DOMAIN=lutecium.app, lutecium.duckdns.org

# Sous-domaine DuckDNS (sans le .duckdns.org)
DUCKDNS_SUBDOMAIN=lutecium

# Token DuckDNS — récupéré sur https://www.duckdns.org une fois connecté
DUCKDNS_TOKEN=

# Email utilisé pour l'enregistrement Let's Encrypt (S-03)
CADDY_ACME_EMAIL=admin@example.com

# UID/GID de l'utilisateur système exécutant les conteneurs (S-09)
PUID=1000
PGID=1000

# Backend lutecium-api (§6 CDC technique). Défauts déjà codés en dur dans app/config.py ;
# seul SECRET_KEY doit être régénéré en prod (signature des sessions et des liens de
# téléchargement, S-04/F-24) : python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production

# Bootstrap du compte admin (A-01) : créé au démarrage si aucun admin n'existe encore.
# Laisser vide en dehors de la configuration initiale.
# ADMIN_PSEUDO=
# ADMIN_PASSWORD=

# Fichier cookies.txt (format Netscape) pour les sites qui l'exigent (M-04) — voir README.
# Déposer le fichier dans deploy/cookies/cookies.txt (monté ro dans le conteneur) puis
# décommenter la ligne suivante.
# COOKIES_FILE=/app/cookies/cookies.txt
