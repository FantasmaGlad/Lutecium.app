# TRACKING — Lutecium

> **Fichier de pilotage multi-sessions.** À lire en **début de session**, à mettre à jour **en temps réel** (au moment de prendre/finir une tâche, pas en fin de session).
> Protocole : [CLAUDE.md](CLAUDE.md) · Plan technique : [docs/PLAN.md](docs/PLAN.md) · Exigences : [docs/cahier-des-charges.md](docs/cahier-des-charges.md)

**Légende :** ⬜ à faire · 🔄 en cours (session + date en colonne Session) · ✅ fait · 🧪 fait, à vérifier · ⛔ bloqué (raison en Notes)

## Vue d'ensemble

| Phase | Intitulé | Avancement | Critère de sortie (CDC §12) |
|---|---|---|---|
| T | Setup transversal | 3/3 ✅ | — |
| 1 | Cœur applicatif (backend) | 16/16 ✅ | YouTube + TikTok téléchargés de bout en bout |
| 2 | Comptes et quotas | 9/9 ✅ | Invité : 1 téléchargement puis invitation ; quotas appliqués |
| D | Design (Claude Design) | 5/6 (+1 🧪) | Livrables CDC UI §12 validés |
| 3 | Frontend React | 13/14 (+1 🧪) | Parcours complet clavier/mobile en français |
| 4 | Salle de contrôle (admin) | 7/7 ✅ | Suspension + purge disque en ≤ 3 clics |
| 0 | Socle serveur (Wyse) | 4/4 ✅ | HTTPS répond depuis l'extérieur |
| 5 | Industrialisation | 5/8 (+2 🧪) | Réinstallation de zéro < 30 min |

## Phase T — Setup transversal

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| T-01 | Structure du dépôt (backend/, frontend/, deploy/, docs/) + git init + .gitignore + .env.example | §9 | ✅ | S1 (2026-07-20) | backend/ scaffoldé (app/api,core,models,services + tests), .env.example racine créé (variables §6). frontend/ reste à créer en Phase 3 |
| T-02 | Docs de pilotage : cahier des charges, PLAN.md, TRACKING.md, CLAUDE.md | — | ✅ | S1 (2026-07-20) | |
| T-03 | README squelette | — | ✅ | S3 (2026-07-24) | Fusionné avec P5-07 (voir Phase 5) : README complet directement plutôt qu'un squelette intermédiaire |

## Phase 1 — Cœur applicatif (backend)

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| P1-01 | Squelette FastAPI : app factory, config.py (variables §6), /api/health | P-02 | ✅ | S1 (2026-07-20) | pyproject.toml, app/main.py, app/config.py (pydantic-settings), /api/health testé (pytest + requête manuelle : `{"status":"ok","app_version":"0.1.0","yt_dlp_version":"2026.7.4"}`). Dev local sur Python 3.14 (3.12 non dispo sur la machine de dev) ; le Dockerfile (P1-02) restera la cible officielle python:3.12-slim |
| P1-02 | Dockerfile API (python:3.12-slim + ffmpeg, non-root) + compose dev | S-09 | ✅ | S1 (2026-07-20) | backend/Dockerfile + .dockerignore, deploy/docker-compose.dev.yml (hot-reload). Buildé et testé sur le Wyse (pas de Docker en local) : /api/health OK, utilisateur non-root confirmé (uid=1000 lutecium), ffmpeg 7.1.5 présent. Image de test supprimée après validation, rien laissé sur le serveur |
| P1-03 | POST /api/analyze : extract_info, refus playlists, formats+fps, nom proposé | F-10, F-11, F-15 | ✅ | S1 (2026-07-20) | app/api/analyze.py + core/ytdlp.py + core/filenames.py. Tests unitaires mockés (4 passent) + tests réels contre YouTube (vraie vidéo : formats/audio/sous-titres complets en 1,6s ; vraie playlist : rejetée en 2,5s). Bug détecté et corrigé pendant le test réel : sans `extract_flat=\"in_playlist\"`, yt-dlp tentait de résoudre toute la playlist avant de la rejeter (timeout 30s+) |
| P1-04 | File FIFO persistée en BDD (table downloads), reprise au redémarrage | F-20, P-03 | ✅ | S1 (2026-07-20) | SQLAlchemy 2.x async (aiosqlite), modèle `Download` (schéma §8 partiel, Alembic viendra en P2), `POST /api/downloads` (position FIFO), réconciliation au démarrage (P-03 : downloading/processing orphelins → failed, queued repris tels quels). 7 tests unitaires (DB en mémoire) + vérification manuelle sur fichier SQLite réel. Bug corrigé pendant le test réel : le répertoire `data/` n'était pas créé avant l'ouverture du fichier SQLite |
| P1-05 | Worker téléchargement vidéo + fusion ffmpeg + hooks de progression | F-12, F-14 | ✅ | S1 (2026-07-20) | core/worker.py (pool de N=MAX_CONCURRENT_DOWNLOADS coroutines consommant une asyncio.Queue), core/events.py (bus pub/sub thread-safe pour SSE, P1-08 branchera dessus), run_download dans core/ytdlp.py. **Testé en réel de bout en bout** (pas seulement mocké) : vraie vidéo YouTube téléchargée + fusionnée (ffprobe confirme pistes h264+opus), size_bytes exact, chemin d'échec vérifié (mauvais ID vidéo → statut failed + message FR). Mode `video` uniquement pour l'instant ; audio seul / sous-titres = P1-06 |
| P1-06 | Modes audio seul (mp3/m4a/opus) et sous-titres seuls | F-12 | ✅ | S1 (2026-07-20) | run_download refactoré en dispatch par mode (video/audio/subtitles). Audio : FFmpegExtractAudio. Sous-titres : writesubtitles+skip_download. `mode` validé par un Literal côté API. Testé en réel : mp3 produit (ffprobe confirme codec mp3), .vtt anglais produit (contenu WebVTT valide) |
| P1-07 | Nom de fichier personnalisé assaini (whitelist) | F-13, S-05 | ✅ | S1 (2026-07-20) | Assaini côté API (sanitize_filename) avant stockage en options, utilisé comme outtmpl par le worker. Testé en réel : `"Mon Fichier : Test / Perso"` → fichier `Mon Fichier Test Perso.mp4` produit |
| P1-08 | SSE /api/downloads/{id}/events (position, %, vitesse, étape ffmpeg) | F-21 | ✅ | S1 (2026-07-20) | sse-starlette, snapshot initial selon le statut DB puis abonnement au bus (events.py) jusqu'à un événement terminal. Testé en réel avec `curl -N` : downloading → progress (bytes/speed/eta réels) → done (avec file_url), et reconnexion après coup renvoie directement l'état final |
| P1-09 | Temps estimé (moyenne glissante des débits récents) | F-22 | ✅ | S1 (2026-07-20) | core/throughput.py : moyenne glissante des N=10 dernières durées de job, estimation = ceil(position/slots)×moyenne. Testé en réel : `estimated_wait_seconds` passe de `null` (aucun historique) à une valeur cohérente après un premier job terminé |
| P1-10 | Annulation (en file et en cours) + nettoyage immédiat | F-23, F-32 | ✅ | S1 (2026-07-20) | core/cancellation.py (flag coopératif), vérifié dans le hook de progression → `DownloadCancelledError`. Testé en réel : job annulé en cours de téléchargement → statut `cancelled`, répertoire supprimé immédiatement (confirmé : hook exception propage bien à travers yt-dlp sans être ré-encapsulée) |
| P1-11 | Lien signé + GET /api/files/{token} | F-24 | ✅ | S1 (2026-07-20) | itsdangerous (URLSafeTimedSerializer, expiration = FILE_TTL_MINUTES). Testé en réel : fichier téléchargé via le lien signé, taille et Content-Disposition corrects ; token invalide → 410 |
| P1-12 | TTL 5 min après complétion + purge filet 15 min | F-30, F-31 | ✅ | S1 (2026-07-20) | core/cleanup.py : boucle toutes les 30s, expire les `done` plus vieux que FILE_TTL_MINUTES → `expired` + suppression fichiers ; filet de sécurité à 3×FILE_TTL_MINUTES (15 min par défaut) purge tout répertoire orphelin. Tests pytest dédiés (DB + tmp_path) |
| P1-13 | Limites : MAX_FILE_SIZE_GB (pré-check + interruption), cap disque global, MAX_QUEUE_SIZE | §6 | ✅ | S1 (2026-07-20) | `max_filesize` yt-dlp (pré-check + interruption en un seul mécanisme natif). Testé en réel avec limites très basses : file pleine → 429, cap disque dépassé → 507, fichier trop volumineux → échec avec message clair (bug de message trouvé et corrigé pendant le test : yt-dlp écarte silencieusement les formats trop lourds sans exception) |
| P1-14 | Messages d'erreur français (mapping exceptions yt-dlp) | F-16 | ✅ | S1 (2026-07-20) | core/errors.py : mapping par motifs (vidéo privée, indisponible, géo-bloqué, cookies requis, site non supporté, introuvable, réseau) avec repli générique ; message + `raw` (détail technique) conservés séparément |
| P1-15 | Rate limiting analyse : 10/min/utilisateur | §6 | ✅ | S1 (2026-07-20) | Fenêtre glissante en mémoire par IP (core/ratelimit.py). Testé en réel : 12 requêtes rapides → 10 traitées, 2 dernières en 429 |
| P1-16 | Critère de phase : YouTube + TikTok de bout en bout + tests pytest | §12 | ✅ | S1 (2026-07-20) | **Critère de sortie de la Phase 1 atteint.** YouTube : analyse + téléchargement + fusion vérifiés (ffprobe : h264+opus). TikTok : analyse + téléchargement réels vérifiés (ffprobe : hevc+aac, vidéo publique réelle). 26 tests pytest passent (file, TTL, assainissement, signature, débit, rate limit, événements, analyse, santé). Mode `video` complet ; audio/sous-titres également validés en réel (P1-06) |

## Phase 2 — Comptes et quotas

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| P2-01 | Alembic init + migration schéma complet §8, SQLite WAL | §8 | ✅ | S1 (2026-07-20) | Modèles User/Session/GuestDownload/DailyUsage/Setting. App réelle applique les migrations au démarrage (create_all réservé aux tests). Testé en réel : 7 tables créées, WAL confirmé actif |
| P2-02 | Inscription libre + hachage argon2id | F-01..03, S-04 | ✅ | S1 (2026-07-20) | argon2-cffi (argon2id). Testé en réel (curl+cookie jar) |
| P2-03 | Login/logout, sessions 30 j, cookies httpOnly/Secure/SameSite | F-04, S-04 | ✅ | S1 (2026-07-20) | /api/auth/{register,login,logout,me,change-password}. Cookie httpOnly+Secure+SameSite=Lax confirmé en réel. `settings.secure_cookies` ajouté (désactivable en dev http) |
| P2-04 | Verrouillage brute-force (5 échecs) | S-06 | ✅ | S1 (2026-07-20) | Compteur pseudo+IP en mémoire, 15 min. Testé en réel : 429 après 5 échecs |
| P2-05 | Mode invité : cookie signé + hash IP, 1 téléchargement, purge quotidienne | F-06..08 | ✅ | S1 (2026-07-20) | Soft limit cookie+IP (hash salé quotidien), purge quotidienne (guest_downloads). Testé en réel : 1er OK, 2e refusé, contournement par suppression du cookie détecté par l'IP |
| P2-06 | Quota journalier + quota individuel + admin exempté + quota-cadeau (la demande qui franchit la limite est acceptée, les suivantes refusées) | §6, A-02, UI §6.4 | ✅ | S1 (2026-07-20) | Jour civil, minuit (tranché 2026-07-20). Testé en réel : téléchargement-cadeau accepté (dépasse le quota), 2e refusé avec le message exact de la spec UI |
| P2-07 | Reset mdp par l'admin → mdp temporaire + changement forcé | F-05 | ✅ | S1 (2026-07-20) | POST /api/admin/users/{id}/reset-password. Testé (parcours HTTP complet) : reset → connexion mdp temporaire → changement forcé sans ancien mdp → ancien mdp invalidé |
| P2-08 | Table settings + config runtime (BDD prime sur .env) | §8, A-14 | ✅ | S1 (2026-07-20) | Cache mémoire rafraîchi au démarrage + toutes les 30s, GET/PATCH /api/admin/settings (whitelist). Testé en réel : override max_queue_size=1 posé par un admin, effet immédiat vérifié sur un vrai POST /api/downloads |
| P2-09 | Critère de phase : parcours invité + quotas vérifiés | §12 | ✅ | S1 (2026-07-20) | **Critère de sortie de la Phase 2 atteint.** Parcours complet réel : invité télécharge 1 fichier → 2e refusé (invitation à s'inscrire) → inscription → limite invité levée → quota du compte correctement mis à jour. 46 tests pytest passent |

## Phase D — Design (Claude Design)

Livrables du CDC UI §12, à réaliser avec Claude Design avant/pendant la Phase 3.

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| D-01 | Design system léger : tokens (2 thèmes, typo, espacements, rayons) + composants de base | UI §2, §12.1 | ✅ | S3 (2026-07-24) | Monospace tranchée : **JetBrains Mono**, auto-hébergée via `@fontsource/jetbrains-mono` (400/500/700, latin subsetté automatiquement, ~43 Ko réels côté client), plutôt que de dépendre d'une police système aléatoire selon l'OS — c'est elle qui porte l'identité terminal (CDC §2.3). Sans-serif : décision explicite de garder la police système (pas d'auto-hébergement, rôle de confort seulement, cf. option acceptée par le CDC). Bibliothèque d'animation confirmée : Framer Motion (déjà en place depuis P3). Audit des espacements/rayons : aucune valeur codée en dur hors tokens (sauf micro-ajustements <4px sur les badges denses de l'admin, cohérents avec CDC §2.4). Formalisé dans [docs/design-system.md](../docs/design-system.md) (nouveau, source de référence versionnée : palette corrigée par D-05, typo, échelle, catalogue des familles de composants). Vérifié en réel : police chargée et appliquée dans les deux thèmes (`document.fonts`, `getComputedStyle`), `tsc`/lint/build propres |
| D-02 | Wordmark : 3 pistes explorées, 1 retenue, déclinaisons (header, favicon, icône PWA) | UI §2.1, §12.2 | ✅ | S3 (2026-07-24) | **Piste retenue confirmée** : `lutecium▌` (piste 1 du CDC, curseur bloc clignotant 1,1s, coupé si `prefers-reduced-motion` — déjà implémenté en P3-01, vérifié conforme). Déclinaison compacte `lu>` (esprit piste 3) gardée pour favicon/PWA — hybride assumé, cohérent avec la consigne « déclinée ». **Comblé le vrai manque flagué depuis P3-12** (« pas d'export PNG, aucun outil disponible en session ») : icônes PWA rasterisées en PNG via un `<canvas>` navigateur (192/512, + variante maskable avec zone de sécurité correctement dimensionnée — l'ancien SVG maskable n'avait pas de marge de sécurité explicite) — `icon-192.png`, `icon-512.png`, `icon-maskable-192.png`, `icon-maskable-512.png`, `apple-touch-icon.png` (180×180). `manifest.webmanifest` mis à jour (PNG en plus du SVG `any`). **Bug corrigé au passage** : `apple-touch-icon` pointait vers le SVG, non fiable sur iOS Safari (support SVG quasi inexistant pour cette balise) → PNG dédié. Vérifié en réel : PNG relus et inspectés visuellement (rendu net, zone de sécurité respectée pour le maskable), manifest JSON valide, `tsc`/lint/build propres, fichiers bien copiés dans `dist/` |
| D-03 | Maquettes HD mobile+desktop, 2 thèmes : états A→F, gestionnaire, auth, historique/compte, invité, admin | UI §12.3 | 🧪 | S3 (2026-07-24) | Périmètre adapté : les 6 états, le gestionnaire, l'auth, l'historique/compte, l'invité et l'admin ont tous déjà été construits et testés en réel dans le navigateur (2 thèmes, mobile+desktop) au fil des sessions S2 (P3-02 à P3-13, P4-06) — pas de nouvelles maquettes à produire, l'implémentation fait déjà foi. Vérification faite aujourd'hui, ciblée sur les changements de cette session (tokens D-05, police D-01, mascotte D-06) : **aucune couleur codée en dur hors `tokens.css`** dans tout `frontend/src` (CSS et TSX, grep exhaustif) — garantit que le correctif de contraste D-05 s'applique bien partout sans exception ratée. Pages critiques revérifiées fonctionnelles avec les nouveaux tokens/police (login, redirection `/historique`→`/login` sans session, redirection `/admin`→`/` sans rôle admin), aucune erreur console. **Non fait aujourd'hui** : nouvelle galerie de captures d'écran — l'outil de capture du panneau navigateur est resté bloqué toute la fin de session (timeout systématique, y compris sur une page externe neutre sans rapport avec l'app, donc panne d'infrastructure de l'outil et non un bug applicatif) ; statut 🧪 en conséquence, à reprendre si l'outil se rétablit ou lors d'une prochaine session |
| D-04 | Prototype des animations du moment signature (B→E + quota-cadeau) | UI §9, §12.4 | ✅ | S3 (2026-07-24) | **Vrai manque trouvé** : seul `DoneCard` (état E) avait une animation d'entrée Framer Motion — les transitions A→B→C→D étaient des remplacements instantanés sans morphing, alors que le CDC en fait explicitement la priorité n°1 du budget d'animation. Corrigé : `MainFlow.tsx` enveloppe désormais tout le bloc central dans `AnimatePresence mode="wait"` (clé = état + sous-statut du job), fondu+glissement discret (0,22s) sur chaque transition A→F, cohérent avec les micro-animations propres de chaque carte (ex. célébration de `DoneCard`) plutôt que de les remplacer. Quota-cadeau (`AccountPage`/`AccountMenu`) : jauge déjà animée en interpolation (largeur 0→pct, 0,6s) + message qui apparaît en fondu — **raffinement de la déviation notée en P3-11** : la jauge est désormais plafonnée à 130 % au lieu de 100 %, avec `overflow: visible` sur la piste, pour réellement « dépasser élégamment le maximum » comme demandé au lieu d'être plafonnée au bord du conteneur. Vérifié en réel dans le navigateur (transition idle→analyse→erreur observée avec le fondu/glissement fonctionnel, `rafCalls` confirmés non nuls) après avoir isolé une dégradation de performance propre à l'outil de capture d'écran de cette session (onglet réutilisé trop longtemps, sans rapport avec le code — un onglet neuf a immédiatement confirmé le rendu correct). **Non revérifié visuellement en réel** : le débordement à 130 % de la jauge quota-cadeau (nécessite un compte réel en dépassement, pas de backend disponible ici) — logique CSS/JS simple et à faible risque, à confirmer au prochain vrai dépassement de quota. `tsc`/lint/build propres |
| D-05 | Vérification contrastes WCAG AA sur maquettes finales | UI §11, §12.5 | ✅ | S3 (2026-07-24) | Audit calculé (luminance relative WCAG) de toutes les paires token/usage réel des deux thèmes. **3 échecs réels trouvés et corrigés** dans `tokens.css` : (1) `--color-text-secondary` en thème clair (`#b3b3b3`, ratio 1.79–2.10) — utilisé très largement comme texte informatif réel (tailles/vitesses/dates/en-têtes de tables admin), pas juste décoratif comme le CDC l'espérait → `#6a6a6a` (4.62–5.41, AA texte normal). (2) `--color-success` en thème clair (`#4f8a63`, ratio 4.08) — sous le seuil 4.5 pour le message quota-cadeau (texte régulier 0.9rem, l'élément signature du §6.4) → `#3a6b4a` (6.21–6.26). (3) `--color-surface-alt` utilisé comme bordure quasi partout (cartes, champs, drawers) mais ratio non-texte 1.27–1.96 sur les deux thèmes (seuil WCAG 1.4.11 : 3:1) → sombre `#767676` (3.16–3.83), clair `#868686` (3.11–3.64). Effet de bord détecté et corrigé : la jauge quota-cadeau du menu compte (fill `--color-success` sur piste `--color-surface-alt`) est passée sous 3:1 avec le nouveau `surface_alt` → liseré `--color-accent` ajouté sur cet état plutôt que de complexifier encore la palette (cohérent avec la consigne CDC §2.2 « jamais des aplats » pour success/error). Tous les autres textes (principal, erreur, texte sur `--color-accent`) étaient déjà conformes. Vérifié en réel : valeurs calculées + confirmées sur les éléments rendus (`getComputedStyle`, bordure du champ URL) dans les deux thèmes après le correctif |
| D-06 | Mascotte 3D (hamster) : élément de décor déplaçable et tournable | — (hors CDC, décision utilisateur) | ✅ | S3 (2026-07-24) | `frontend/src/components/Mascot.tsx` : modèle fourni par l'utilisateur (`hamster-low-poly.obj`/`.mtl`, choisi explicitement plutôt que le `.glb` disponible en alternative) servi depuis `frontend/public/models/`, rendu via `@react-three/fiber`+`@react-three/drei`+`three-stdlib` (nouvelles dépendances). Monté dans `PublicLayout` (persiste sur toutes les pages publiques, pas sur l'admin), purement décoratif (`aria-hidden`). Rotation : `OrbitControls` (glissé direct sur le modèle + légère rotation auto au repos, désactivée si `prefers-reduced-motion`). Déplacement : poignée dédiée en bas de la carte (`useDragControls`/`dragListener=false`), contraint à la fenêtre. **Bug réel trouvé et corrigé en testant** : un `stopPropagation()` sur le canevas ne suffisait pas à empêcher qu'un glissé de rotation déclenche *aussi* le déplacement de toute la carte (les deux gestes partent du même pointerdown) — corrigé avec le pattern poignée dédiée de Framer Motion, vérifié en réel (azimut de rotation et position de la carte changent indépendamment selon la zone glissée). CSP (P5-04) revérifiée sans violation avec cette nouvelle dépendance. **Déviation assumée** : le hamster a ses couleurs propres (blanc/rose/noir), pas strictement monochrome comme le reste du CDC UI §2.2 — décision utilisateur explicite pour cet unique élément de décor. Bundle JS +~1 Mo (three.js) : accepté pour aujourd'hui, code-splitting (`import()` dynamique) à envisager si ça devient gênant |

## Phase 3 — Frontend React

Spécification : [CDC UI/UX](docs/ui-ux-cahier-des-charges.md). Mobile-first, deux thèmes monochromes, TypeScript.

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| P3-01 | Squelette Vite + React + TS, tokens design system, thèmes sombre/clair + toggle + prefers-color-scheme | UI §2 | 🧪 | S1 (2026-07-20) | Détour demandé par l'utilisateur avant la suite du backend (voir § Décisions). `frontend/` scaffoldé, `src/styles/tokens.css` (palette 2 thèmes CDC §2.2, typo §2.3), toggle thème fonctionnel (testé clair/sombre), tab title `lutecium▌` + favicon `lu>`. Wordmark et couleurs choisis directement (option 1 du CDC §2.1) sans passer par une session Claude Design formelle → à revalider/raffiner en Phase D. Pas de Framer Motion ni composants avancés à ce stade |
| P3-02 | Layout global : header (burger, wordmark, toggle, compte) + drawer navigation | UI §3.1 | ✅ | S2 (2026-07-21) | Burger câblé sur `NavDrawer` (Accueil, Mon historique, Mon compte, Admin si rôle admin), fermeture Échap + clic scrim + focus auto sur le 1er lien. Bouton compte devenu `AccountMenu` (jauge quota, historique, compte, déconnexion si connecté ; lien connexion sinon). Testé en réel dans le navigateur |
| P3-03 | États A/B : champ URL, bouton coller, analyse auto dès URL valide, erreurs inline | F-10, UI §4 | ✅ | S2 (2026-07-21) | `UrlCard` relié à `/api/analyze` (debounce 400ms sur URL valide détectée), animation de scan (points suspensifs), erreur inline factuelle (message FR du backend), champ reste éditable. Testé en réel avec de vraies vidéos YouTube (succès et échecs réels : vidéo indisponible, site nécessitant des cookies) |
| P3-04 | État C : aperçu, Télécharger / Audio seul, options avancées, nom de fichier, poids estimé | F-11..13, UI §4 | ✅ | S2 (2026-07-21) | `PreviewCard` : miniature/titre/durée/site, actions Télécharger/Audio seul, options avancées repliées (qualité vidéo réelle avec fps/codec/poids, format audio, sous-titres détectés, nom de fichier éditable), poids estimé toujours visible. Testé en réel |
| P3-05 | États D/E/F : progression SSE, annulation, célébration, compte à rebours TTL, erreurs actionnables | F-21..24, UI §4 | ✅ | S2 (2026-07-21) | `ProgressCard` (SSE natif via `EventSource`, position+ETA en file, %/vitesse/poids en direct, étape ffmpeg), `DoneCard` (célébration Framer Motion, bouton Enregistrer, compte à rebours TTL 5 min côté client — **déviation notée** : démarre à la réception de l'événement `done`, pas à l'horodatage serveur exact, TTL non renvoyé par l'API), `ErrorCard` (message factuel + réessayer/changer d'URL + détails repliables). Testé en réel de bout en bout (téléchargement vidéo réel, %, vitesse, poids, fichier final récupéré) |
| P3-06 | Gestionnaire de téléchargements (barre repliée, badge, liste temps réel, actions par ligne) | UI §5.2 | ✅ | S2 (2026-07-21) | `DownloadManagerContext` (SSE centralisé par job, découplé du bloc central — une tâche en cours survit au retour à l'état A) + `DownloadManagerDrawer` (barre repliée + badge, liste animée Framer Motion, actions enregistrer/annuler/réessayer/retirer). Testé en réel : job visible en direct pendant progression, retélécharger depuis l'historique alimente la même liste |
| P3-07 | Toasts + notifications navigateur (permission au 1er téléchargement en file) | UI §5.1 | ✅ | S2 (2026-07-21) | `ToastContext`/`Toasts` (empilables, auto-dismiss 5s). `requestNotificationPermissionOnce` déclenché à la 1ère mise en file (pas à l'arrivée sur le site) ; `notifyDownloadReady` uniquement si onglet caché. Toast testé en réel (retélécharger depuis l'historique) |
| P3-08 | Pages auth : login/register, changement mdp forcé, reprise de l'URL en attente | F-01..05, UI §6.2 | ✅ | S2 (2026-07-21) | `/login`, `/register`, `/changer-mot-de-passe` (forcé via `must_change_password`, redirection automatique). Reprise de l'URL en attente via `sessionStorage` (posée quand un invité est bloqué, consommée au montage de `MainFlow`) : testée en réel (invité bloqué → inscription → analyse relancée automatiquement sur l'URL en attente) |
| P3-09 | Parcours invité : carte d'invitation post-téléchargement, blocage doux | F-06, UI §6.1 | ✅ | S2 (2026-07-21) | `GuestInvite` affichée sous `DoneCard` après le 1er téléchargement invité, et à la place de l'analyse dès le 2e lien collé (détection client via `localStorage` + code d'erreur `guest_limit_reached` en filet). Testé en réel : 1er téléchargement invité OK + invitation affichée, 2e lien collé → invitation immédiate sans appel à `/api/analyze` |
| P3-10 | /historique (avec retélécharger) + /compte (jauge quota, état dépassé) | UI §6.3 | ✅ | S2 (2026-07-21) | Nécessitait `GET /api/me/downloads`, absent de la Phase 2 malgré le contrat PLAN §3 → ajouté côté backend cette session (voir Phase 2). `/historique` : liste (titre=nom de fichier, site, poids, date, statut) + retélécharger (relance `POST /api/downloads` et alimente le gestionnaire). `/compte` : pseudo, jauge quota, changement mdp, déconnexion. **Déviation** : « titre » de la spec UI affiché comme nom de fichier (aucune colonne `title` en base, le nom de fichier assaini en tient lieu). Testé en réel (compte réel, téléchargement, historique, retélécharger) |
| P3-11 | Quota-cadeau : jauge qui dépasse + animation + messages dédiés | UI §6.4 | ✅ | S2 (2026-07-21) | Jauge `/compte` et menu compte : au-delà du quota, couleur dédiée + message animé (Framer Motion) « Celle-ci est pour nous. Quota dépassé — à demain ! », exactement le texte de la spec. Message de refus factuel déjà fourni par le backend (P2-06). **Déviation mineure** : la barre est visuellement plafonnée à 100 % (pas de dépassement physique du conteneur) — à raffiner en Phase D. Vérifié en réel (usage du jour forcé au-delà du quota en base de dev) |
| P3-12 | PWA : manifest, icône, share target Android (URL pré-collée + analyse lancée) | UI §8 | ✅ | S2 (2026-07-21) | `manifest.webmanifest` (icônes `lu>` en SVG — **déviation** : pas d'export PNG, aucun outil de génération d'image disponible en session, à refaire en Phase D), service worker minimal sans cache (installabilité uniquement, pas de hors-ligne). `share_target` GET (`title`/`text`/`url`) → `takeSharedUrl()` extrait l'URL et relance l'analyse automatiquement. Testé en réel : navigation avec `?url=...` déclenche bien l'analyse automatique sans étape manuelle ; SW confirmé actif dans le navigateur |
| P3-13 | Accessibilité : AA, clavier complet, aria-live, prefers-reduced-motion | UI §11 | ✅ | S2 (2026-07-21) | Cibles tactiles remontées à 44px (toggle thème, burger, bouton compte, coller, actions principales C/D/E). `MotionConfig reducedMotion="user"` (Framer Motion) en plus de la règle CSS globale déjà posée en P3-01. `aria-live` sur la progression (D, demandé explicitement par le CDC) ; countdown TTL (E) volontairement hors aria-live (annonce ponctuelle « ton fichier est prêt » séparée pour éviter le bruit d'une re-lecture chaque seconde). Drawers/menus : fermeture Échap + focus déplacé à l'ouverture, pas de piège à focus (conforme, un piège est explicitement interdit par le CDC). Rôle `menu` retiré du menu compte (implémentation incomplète du pattern ARIA menu jugée pire que l'absence de rôle) au profit d'un simple disclosure `aria-expanded`/`aria-controls`. Vérifié en réel (navigateur) sauf l'ordre de tabulation clavier, non vérifiable dans cet environnement de test automatisé (Tab ne déplace pas le focus dans le harnais utilisé) — repose sur l'ordre naturel du DOM avec des éléments HTML natifs, à revérifier manuellement |
| P3-14 | Build production servi par Caddy | §3.3 | ✅ | S3 (2026-07-24) | **Synchronisation complète effectuée.** Le Wyse servait encore le squelette du 20 juillet (avant toute la Phase 3/4) — `frontend/dist` (build du jour, mascotte/design system inclus) et `backend/` (Phase 4 + tout S3) resynchronisés par rsync, `deploy/` (Caddyfile CSP, docker-compose.yml durci, cron/, provision/) synchronisé aussi. **Bug réel rencontré et corrigé pendant le déploiement** : décalage temporaire frontend neuf / backend pas encore basculé (`docker compose build` fait mais pas `up -d`) → 422 sur `/api/analyze` en prod, corrigé en terminant le déploiement. Vérifié en réel de bout en bout dans un vrai navigateur après coup : collage d'URL → analyse auto → carte d'aperçu correcte (titre, miniature, durée, poids). Process manuel (rsync), pas encore automatisé (P5-01 reste un script à valider sur une install neuve) |

## Phase 4 — Salle de contrôle (admin)

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| P4-01 | API gestion utilisateurs (liste, suspension, suppression, quota, reset mdp) + vue invités | A-10 | ✅ | S2 (2026-07-21) | GET/PATCH/DELETE `/api/admin/users`, GET `/api/admin/guests` (IP déjà anonymisée en hash salé quotidien, aucun changement nécessaire). Suppression : sessions supprimées, téléchargements détachés (`user_id=NULL`, conservés pour les métriques globales) plutôt que supprimés. Reset mdp déjà fait en P2-07. Testé en réel (suspension → connexion refusée 403, quota individuel appliqué, suppression confirmée) + 6 tests pytest |
| P4-02 | API métriques d'usage (jour/semaine, volume, top sites, taux d'erreur, file) | A-11 | ✅ | S2 (2026-07-21) | GET `/api/admin/metrics` : `app/services/metrics.py` (agrégations SQL). Testé en réel avec de vrais téléchargements |
| P4-03 | API état système (CPU, fréquence, RAM, température, disque) + alerte disque | A-12 | ✅ | S2 (2026-07-21) | GET `/api/admin/system` : `app/core/system.py` (psutil, exécuté via `asyncio.to_thread` — `cpu_percent(interval=…)` bloque sinon la boucle asyncio). Température lue depuis `/sys/class/thermal` ou `/sys/class/hwmon` (montés ro dans `deploy/docker-compose.yml`, `None` si indisponible en dev). Pas d'accès au socket Docker (décision PLAN §1.2-5) : heartbeat applicatif (uptime du process) à la place de l'état par conteneur. Testé en réel : température/CPU/RAM/disque réels de la machine de dev renvoyés |
| P4-04 | Journal téléchargements + erreurs, rotation | A-13, M-02 | ✅ | S2 (2026-07-21) | GET `/api/admin/journal` (paginé, filtrable par statut) sur la table `downloads` existante. M-02 : `app/core/logging_config.py`, `RotatingFileHandler` (5 Mo × 5 fichiers) sur les logs applicatifs, activé une fois par processus. Testé en réel |
| P4-05 | Actions rapides : purge downloads, vidage file, maj yt-dlp | A-14 | ✅ | S2 (2026-07-21) | `purge_all_download_files` (ne touche jamais un job actif), `clear_queue` (annule file d'attente + jobs en cours via le même mécanisme coopératif que l'annulation individuelle), `update_yt_dlp` (`pip install --upgrade`, refusé avec 409 si des jobs actifs). **Déviation** : le « rechargement propre des workers » de PLAN.md n'est qu'un rechargement de la chaîne de version (`importlib.reload`) — une mise à jour complète des extracteurs yt-dlp nécessite un redémarrage du conteneur (cohérent avec le cron nightly M-01 qui rebuild l'image). Testé en réel (les 3 actions, y compris un vrai `pip install --upgrade yt-dlp`) |
| P4-06 | UI dashboard : 4 sections (Vue, Users, Sys, Logs), style mission control monochrome, SSE temps réel | A-10..14, UI §7 | ✅ | S2 (2026-07-21) | `/admin` (garde de rôle, redirection silencieuse vers `/` sinon — vérifié en réel déconnecté). Sidebar 4 sections (`▣ Vue ▢ Users ▢ Sys ▢ Logs`), monospace, desktop-first. Vue : tuiles chiffres, histogramme 7 jours (SVG maison, pas de lib de charts), top sites, file en direct — abonné au flux SSE `/admin/metrics/stream`. Système : jauges circulaires CPU/RAM/disque (SVG), température, fréquence, actions rapides — abonné à `/admin/system/stream`. Users : table dense + actions par ligne (suspendre/quota/reset mdp/supprimer avec confirmation), vue invités. Logs : table filtrable par statut + recherche client. **Déviation** : confirmations/saisies via `window.confirm`/`window.prompt` natifs plutôt que des modales stylées (fonctionnel, cohérent clavier, mais visuellement hors charte — à raffiner en Phase D). Testé en réel : suspension d'un compte de test → connexion refusée (403) confirmée par API, suppression confirmée, toutes les sections vérifiées avec de vraies données (téléchargements, invité, système réel de la machine de dev) |
| P4-07 | Critère de phase : suspension + purge en ≤ 3 clics | §12 | ✅ | S2 (2026-07-21) | **Critère de sortie de la Phase 4 atteint.** Suspension : Users → suspendre (2 clics depuis le dashboard). Purge : Sys → Purger les fichiers → confirmer (3 clics). Vérifié en réel dans le navigateur |

## Phase 0 — Socle serveur (Wyse accessible en SSH par clé ✅)

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| P0-01 | Script minimal : durcissement SSH, ufw, fail2ban, unattended-upgrades | S-01, S-02, S-08, S-10 | ✅ | S1 (2026-07-20) | deploy/provision/phase0.sh — vérifié : password auth off, root off, ufw (22=LAN, 80/443 publics), fail2ban + unattended actifs |
| P0-02 | Docker + Compose + auto-cpufreq sur Debian 13 | P-01 | ✅ | S1 (2026-07-20) | Docker 29.6.2 + Compose v5.3.1 (dépôt officiel), fanta dans le groupe docker, auto-cpufreq actif |
| P0-03 | Caddy + page de test + DuckDNS + HTTPS Let's Encrypt | S-03 | ✅ | S1 (2026-07-20) | Port forwarding 80/443 confirmé par l'utilisateur sur la Bbox → redémarrage de Caddy → certificat Let's Encrypt obtenu (`certificate obtained successfully`, CN=lutecium.duckdns.org, issuer Let's Encrypt) |
| P0-04 | Critère de phase : HTTPS répond depuis l'extérieur | §12 | ✅ | S1 (2026-07-20) | `https://lutecium.duckdns.org` → 200, certificat valide. Preuve d'accès externe la plus forte : le challenge ACME tls-alpn-01 a été validé par les serveurs Let's Encrypt eux-mêmes (IPs externes visibles dans les logs Caddy) via Internet, pas le LAN. Confirmation complémentaire recommandée : test depuis un téléphone en 4G/5G |

## Phase 5 — Industrialisation

| ID | Tâche | Exigences | Statut | Session | Notes |
|---|---|---|---|---|---|
| P5-01 | install-lutecium.sh complet et idempotent (§9.1–9.7) | §9 | 🧪 | S3 (2026-07-24) | Toutes les étapes constitutives ont maintenant été exercées séparément en réel sur le Wyse (`docker compose up -d --build`, bootstrap admin, pose de la crontab, `configure.sh` déjà validé) — mais le script `install-lutecium.sh` **lui-même** n'a pas été exécuté comme un tout (mise à jour d'une machine déjà provisionnée, pas une install neuve). **Leçon réelle retenue pendant la session** : `docker compose restart` ne relit PAS le `.env` (seul `up -d` recrée le conteneur avec les nouvelles variables) — piège rencontré en essayant d'activer le bootstrap admin ; `install-lutecium.sh` utilise déjà `up -d --build` correctement, donc non affecté, mais bon à savoir pour toute intervention manuelle future. Reste à chronométrer/valider sur une vraie machine vierge (P5-08) |
| P5-02 | Configuration interactive → .env + création compte admin | §9.5, A-01 | ✅ | S3 (2026-07-24) | `deploy/provision/configure.sh` : prompts pour domaine/DuckDNS/ACME/PUID-PGID/quotas/admin, écrit `deploy/.env` (backup horodaté de l'ancien avant écrasement, permissions 600). Idempotent : relit le `.env` existant comme défauts, ne régénère jamais une `SECRET_KEY` déjà posée (invaliderait sessions/liens signés), ne redemande les identifiants admin que sur confirmation explicite. **Bug réel trouvé et corrigé pendant le test en réel** : la relecture du `.env` existant utilisait `source`, qui casse sur des valeurs contenant virgule/espace (`LUTECIUM_DOMAIN=lutecium.app, lutecium.duckdns.org` → « commande introuvable ») — remplacé par un parseur KEY=VALUE manuel. Un 2e bug (boucle infinie si la confirmation du mot de passe admin recevait un EOF au lieu d'une vraie saisie) corrigé au passage. Testé en réel en local : premier lancement, relance avec valeurs modifiées, `SECRET_KEY`/mot de passe admin bien préservés si non touchés, backup créé | 
| P5-06 | Beau terminal : zsh + starship, alias, motd Lutecium | §9.8 | 🧪 | S3 (2026-07-24) | `deploy/provision/terminal.sh` : installe zsh (+ `chsh`), starship (script officiel), ajoute prompt/alias/`lutecium-motd()` dans `.zshrc` via un bloc marqué (idempotent, pas de doublon en cas de relance), bannière statique `/etc/motd`. Génération du bloc `.zshrc` testée en réel (syntaxe validée par `bash -n`, contenu vérifié caractère par caractère, idempotence confirmée) ; fonction `lutecium-motd` (formatage `df -h`) testée en réel. **Non testé** : installation zsh/starship elle-même et `chsh` (nécessitent root sur une vraie machine, non disponible ici) — statut 🧪 |
| P5-03 | Crons : yt-dlp nightly, nettoyage, backup BDD 7 j, DuckDNS | M-01, M-03, P-04 | ✅ | S3 (2026-07-24) | M-01 (yt-dlp) et backup BDD posés en crontab réelle sur le Wyse (`0 */6 * * *` et `0 3 * * *`). **M-03 (nettoyage) et DuckDNS confirmés redondants** (voir notes précédentes), pas de cron à écrire. **`backup-db.sh` testé en réel en production** (première fois avec un vrai `docker compose`) : backup à chaud pendant que l'app tournait, décompressé et relu — 4 utilisateurs retrouvés intacts dans la copie, confirmant que le mode WAL est bien géré correctement |
| P5-04 | Caddyfile final : HSTS, X-Content-Type-Options, CSP | S-07 | ✅ | S3 (2026-07-24) | HSTS et X-Content-Type-Options déjà posés en P0-03 ; CSP ajoutée (`default-src 'self'`, `img-src ... https:` pour les miniatures externes renvoyées par yt-dlp, sans `unsafe-inline`). Vérifiée en réel : build de production (`frontend/dist`) servi en local avec les mêmes en-têtes (petit serveur Python jetable), aucune violation CSP en console, requêtes `/api/*` bien émises (connect-src). Caddy non disponible en local pour valider la syntaxe du Caddyfile (contrainte déjà connue depuis P0-03/P1-02, build/validation toujours faits sur le Wyse) — à revérifier au prochain déploiement |
| P5-05 | Durcissement conteneurs : non-root, réseau interne, volumes ro | S-09 | ✅ | S3 (2026-07-24) | Constat : réseau déjà conforme depuis P0-03, `lutecium-api` déjà non-root. Ajouté : `no-new-privileges` sur les 3 services ; `read_only: true` + tmpfs `/tmp`/`/home/lutecium` sur `lutecium-api`. **Déployé et validé en réel sur le Wyse** : les 3 conteneurs démarrent et fonctionnent normalement avec `read_only`, aucun problème d'écriture rencontré (téléchargement réel réussi, backup BDD réussi) — la lecture du code pour anticiper les écritures (ffmpeg/tmp, cache yt-dlp) s'est révélée juste. Caddy et duckdns laissés à leur configuration par défaut |
| P5-07 | README complet + doc cookies.txt | M-04 | ✅ | S3 (2026-07-24) | README.md racine créé. **Constat en cours de tâche** : le support `cookies.txt` (M-04) n'était en fait pas implémenté côté backend (seuls les cookies de session/invité existaient, sans rapport) — la tâche PLAN.md ne prévoyait que la doc en supposant le support déjà là. Ajouté dans la foulée : `Settings.cookies_file` (`COOKIES_FILE`), câblé en `cookiefile` yt-dlp dans `extract_info` et `_base_opts` (silencieusement ignoré si absent/introuvable), volume `deploy/cookies:/app/cookies:ro` sur `lutecium-api`. 4 tests unitaires ajoutés (63 passent). Non testé en réel avec un vrai fichier cookies.txt (nécessiterait un compte jetable sur un site le exigeant) |
| P5-08 | Critère de phase : réinstallation chronométrée < 30 min | §12 | ⬜ | | |

## Décisions

| Date | Sujet | Décision | Origine |
|---|---|---|---|
| 2026-07-20 | Stockage downloads | Volume disque, pas tmpfs (15 GB vs 16 GiB RAM) | PLAN §1.2-1 |
| 2026-07-20 | Quota glissant | ~~Calculé sur `downloads` (24 h glissantes)~~ **Superseded**, voir ligne « Quota journalier » ci-dessous | PLAN §1.2-2 |
| 2026-07-20 | Architecture worker | Workers asyncio in-process, file persistée en BDD | PLAN §1.2-3 |
| 2026-07-20 | Alembic | Introduit en Phase 2 (P1 = create_all sur `downloads`) | PLAN §1.3 |
| 2026-07-20 | Git | Dépôt local, un commit par tâche terminée ; remote GitHub plus tard | Cadrage utilisateur |
| 2026-07-20 | Frontend | TypeScript | Cadrage utilisateur |
| 2026-07-20 | Démarrage | Le plan doit être **validé par l'utilisateur** avant de coder les phases 1–4 ; la préparation du serveur (Phase 0) démarre tout de suite | Cadrage utilisateur |
| 2026-07-20 | Accès Wyse | SSH par clé uniquement (`fanta@192.168.1.186`, hostname `debian-malefique`) ; **aucun mot de passe utilisé ni stocké** ; sudo NOPASSWD activé par l'utilisateur | S-02 |
| 2026-07-20 | CDC UI/UX | v1.0 ajouté (docs/ui-ux-cahier-des-charges.md) : Phase D (design) créée, Phase 3 restructurée en 14 tâches, quota-cadeau intégré à P2-06, `GET /api/me/downloads` ajouté aux contrats | Utilisateur |

| 2026-07-20 | Quota journalier | **Jour civil, remise à zéro à minuit** (et non 24 h glissantes) ; calcul via `daily_usage` | Cadrage utilisateur |
| 2026-07-20 | Domaine | Nom de domaine payant `lutecium.app` acheté sur Vercel, en remplacement de `lutecium.duckdns.org` comme domaine principal (backlog CDC §10.2, réalisé plus tôt que prévu). DNS géré par Vercel (nameservers `ns1/ns2.vercel-dns.com`), enregistrement `ALIAS` à l'apex (`@`) → `lutecium.duckdns.org` : Vercel gère la résolution DNS pure (pas de proxy HTTP), DuckDNS continue de suivre l'IP dynamique. Caddy sert désormais les deux domaines (`lutecium.app` en principal, `lutecium.duckdns.org` conservé en secours) | Utilisateur + doc Vercel |

| 2026-07-20 | Détour UI/UX | Interruption volontaire de l'ordre des phases (Phase 1 backend en cours) pour amorcer la Phase 3/D à la demande de l'utilisateur (« voir le site prendre forme »). Choix de wordmark et palette faits directement dans le code (option 1 du CDC UI §2.1, palette §2.2 telle quelle) plutôt que via une session Claude Design formelle — statut 🧪 (à revalider en Phase D). Reprise du backend (P1-02) ensuite | Demande utilisateur |
| 2026-07-21 | Migration Alembic vs `downloads` legacy | La base SQLite de prod contenait déjà la table `downloads` (créée par `create_all` en Phase 1, avant Alembic). La migration initiale P2-01 tente de la recréer → conflit → conteneur en boucle de redémarrage au premier déploiement de la Phase 2. Corrigé en mettant de côté l'ancienne base (`lutecium.db.pre-phase2.bak` dans le volume `deploy_lutecium_data`, une seule ligne de test éphémère perdue, aucune donnée réelle) et en laissant Alembic construire un schéma propre. Pas de correctif nécessaire dans le script de migration lui-même : une installation neuve (P5-01) part toujours d'une base vide | Constat en session S2 |
| 2026-07-21 | Pas de compte admin existant | Aucun mécanisme de création d'un compte admin n'existe encore (prévu en P5-02, « configuration interactive → .env + création du compte admin »). Développement/test de la Phase 4 fait en promouvant manuellement un utilisateur de test en `role=admin` par SQL direct sur la base de dev locale — **jamais fait en production**, technique de test uniquement. Comblé partiellement dans la foulée : bootstrap au démarrage (`ADMIN_PSEUDO`/`ADMIN_PASSWORD` optionnels, `app/core/bootstrap_admin.py`), idempotent, aucun identifiant par défaut. Le script interactif complet (prompts, écriture du .env) reste du ressort de P5-01/P5-02 | Constat en session S2 |
| 2026-07-24 | Support `cookies.txt` (M-04) manquant | La tâche P5-07 (PLAN.md) supposait que le support technique du fichier `cookies.txt` existait déjà et ne prévoyait que la documentation ; en réalité aucun mécanisme ne câblait de `cookiefile` dans les options yt-dlp. Comblé dans la même tâche plutôt que de documenter une fonctionnalité inexistante : `COOKIES_FILE` (settings), volume `deploy/cookies:/app/cookies:ro`, ignoré silencieusement si absent | Constat en session S3 |
| 2026-07-24 | Mascotte 3D (D-06) | Ajout d'un hamster 3D (modèle fourni par l'utilisateur) comme élément de décor déplaçable/tournable sur toutes les pages publiques — hors périmètre du cahier des charges, demande directe de l'utilisateur en Phase D. Déviation assumée : couleurs propres au modèle (blanc/rose/noir), pas strictement monochrome comme le reste de la charte UI §2.2, limitée à ce seul élément décoratif | Demande utilisateur, session S3 |
| 2026-07-21 | Garantie « toujours la dernière version officielle de yt-dlp » | Question directe de l'utilisateur sur la fiabilité de la mise à jour. Constat : `pip install --upgrade` seul (bouton admin P4-05) ne suffisait pas — yt-dlp est importé comme bibliothèque Python (S-05) et ses sous-modules/extracteurs restent chargés en mémoire, un `importlib.reload` ne les rafraîchit pas de façon fiable ; le paquet était donc mis à jour sur disque mais pas réellement utilisé par le processus en cours. Corrigé : `update_yt_dlp()` déclenche maintenant un auto-redémarrage (`SIGTERM` sur soi-même après un court délai) une fois la mise à jour installée, redémarrage repris proprement par `restart: unless-stopped` en prod — vérifié en réel en local (le process s'arrête, doit être relevé par un superviseur externe, exactement le rôle de Docker en prod). Ajouté : `deploy/cron/update-ytdlp.sh` (M-01, à poser en crontab sur le Wyse, 4h du matin) qui fait la même chose côté hôte via `docker compose exec`+`restart`. Ajouté aussi : `ARG CACHEBUST` dans `backend/Dockerfile` avant l'étape `pip install`, pour qu'un rebuild de l'image (déploiement) ne réutilise jamais une couche Docker mise en cache avec une ancienne version — `pyproject.toml` ne fixe qu'un plancher (`yt-dlp>=2024.11`), sans cache-busting un rebuild sans changement de code aurait pu resservir une résolution PyPI périmée. Limite assumée : une mise à jour en place (pip install dans le conteneur en cours d'exécution) ne survit pas à une *recréation* du conteneur (`docker compose down`/`up`, pas un simple `restart`) — seul un rebuild d'image règle ce cas, d'où le cache-busting | Question utilisateur, session S2 |
| 2026-07-24 | Debian installé avec bureau complet (GNOME) au lieu d'un profil serveur | Cause racine des coupures réseau répétées tout au long du projet (LAN et HTTPS publics tombant simultanément, décrites depuis S1 comme des « incidents transitoires ») : le Wyse se mettait en veille faute de profil serveur choisi à l'installation de Debian 13. Corrigé : `sleep.target`/`suspend.target`/`hibernate.target`/`hybrid-sleep.target` masqués (garantie dure, indépendante de la source de la demande de veille — GNOME, ACPI, logind…), cible par défaut basculée sur `multi-user.target`, `gdm.service` désactivé et arrêté. Paquets desktop (GNOME/Xorg/Firefox) laissés installés (non désinstallés, seulement désactivés) — de l'espace disque pourrait être récupéré plus tard si besoin (~27 Go de disque, 17 Go libres après coup). Vérifié en réel : redémarrage complet du Wyse, boot direct en `multi-user.target` sans session graphique, conteneurs Docker relevés automatiquement (`restart: unless-stopped`) | Demande utilisateur, session S3 |
| 2026-07-24 | `docker compose restart` ne relit pas `.env` | Piège rencontré en essayant d'activer le bootstrap du compte admin sur le Wyse : `docker compose restart lutecium-api` redémarre le process mais garde l'environnement figé au moment de la *création* du conteneur — seul `docker compose up -d` (recrée le conteneur) prend en compte un `.env` modifié. `install-lutecium.sh` utilise déjà `up -d --build`, donc non affecté ; piège pertinent uniquement pour les interventions manuelles futures | Constat en session S3 |

**En attente :** plus rien de bloquant côté déploiement — synchronisation complète effectuée le 2026-07-24 (S3, voir journal). Reste P5-08 (réinstallation chronométrée, nécessite une machine vierge) et éventuellement une nouvelle galerie de captures pour D-03 si l'outil de capture redevient fiable.

## Journal des sessions

| Date | Session | Travail effectué |
|---|---|---|
| 2026-07-20 | S1 | Analyse du cahier des charges, création de docs/PLAN.md, TRACKING.md, CLAUDE.md, docs/cahier-des-charges.md. Cadrage reçu : plan à valider, git local, TypeScript, Wyse prêt. git init + .gitignore + 1er commit. Connexion SSH au Wyse vérifiée par clé (Debian 13, 21 Go libres, Docker absent) ; installation bloquée par sudo-avec-mot-de-passe → question posée. |
| 2026-07-20 | S1 (suite) | NOPASSWD sudo activé par l'utilisateur → Phase 0 exécutée via deploy/provision/phase0.sh : durcissement SSH, ufw, fail2ban, unattended-upgrades, Docker 29.6.2 + Compose v5.3.1, auto-cpufreq — tout vérifié actif (P0-01 ✅, P0-02 ✅). CDC UI/UX v1.0 archivé ; PLAN et TRACKING restructurés (Phase D, Phase 3 en 14 tâches, quota-cadeau, GET /api/me/downloads). |
| 2026-07-20 | S1 (suite 2) | Correction de cohérence PLAN.md §1.2 (arbitrage quota aligné sur la décision « minuit »). Préparation P0-03 : deploy/docker-compose.yml (caddy + duckdns), Caddyfile, page de test, .env.example créés et commités ; copiés sur le Wyse (~/lutecium/deploy), `deploy/.env` créé côté serveur (non commité, email ACME rempli, token DuckDNS vide). `docker compose config` validé. En attente du token DuckDNS de l'utilisateur pour lancer les conteneurs. |
| 2026-07-20 | S1 (suite 3) | Token DuckDNS reçu de l'utilisateur, écrit dans `deploy/.env` sur le serveur (jamais dans git/chat en clair après saisie). `docker compose up -d` : caddy + duckdns démarrés et stables. DuckDNS confirmé fonctionnel (`lutecium.duckdns.org` → `176.150.50.31`, IP publique réelle). Let's Encrypt bloqué par timeout sur le challenge HTTP-01 → port forwarding 80/443 manquant sur la box Bouygues, seule action restante côté utilisateur pour P0-03/P0-04. |
| 2026-07-20 | S1 (suite 4) | Utilisateur confirme le port forwarding 80/443 → 192.168.1.186 sur la Bbox. Redémarrage de Caddy : certificat Let's Encrypt obtenu avec succès (challenge tls-alpn-01 validé par les serveurs Let's Encrypt externes). `https://lutecium.duckdns.org` répond 200 avec certificat valide. **Phase 0 terminée (4/4, P0-01 à P0-04 ✅).** |
| 2026-07-20 | S1 (suite 5) | Utilisateur achète `lutecium.app` sur Vercel. Config DNS : enregistrement `ALIAS` à l'apex → `lutecium.duckdns.org` (après un tour d'essais : CNAME sous-domaine refusé car mauvais Name, puis CNAME à l'apex refusé par Vercel qui impose ALIAS pour ce cas). `LUTECIUM_DOMAIN` mis à jour sur le Wyse (`lutecium.app, lutecium.duckdns.org`), Caddy recréé : certificat Let's Encrypt obtenu pour `lutecium.app`. Les deux domaines répondent 200 en HTTPS. |
| 2026-07-20 | S1 (suite 6) | Démarrage Phase 1 (validation implicite du plan par l'utilisateur, « on continue »). T-01 complété : arborescence backend/ (app/api,core,models,services + tests), .env.example racine. P1-01 fait : squelette FastAPI (app factory, config.py pydantic-settings avec les variables §6), route /api/health, testé par pytest et requête manuelle. venv local avec Python 3.14 (3.12 indisponible sur la machine de dev ; Docker python:3.12-slim reste la cible officielle, P1-02 à venir). |
| 2026-07-20 | S1 (suite 7) | Détour demandé par l'utilisateur : amorce Phase 3/D. `frontend/` scaffoldé (Vite + React + TS), tokens de design (2 thèmes, CDC UI §2.2/§2.3), Header (burger, wordmark animé, toggle thème, bouton compte), État A (champ URL + coller presse-papier + sites supportés). Tab title `lutecium▌`, favicon `lu>`. Vérifié dans le navigateur (thème clair/sombre, mobile 375px) et par `tsc --noEmit` + `npm run build` (OK). P3-01/02/03 marquées 🧪 (amorcées, non finalisées — à raffiner en Phase D). |
| 2026-07-20 | S1 (suite 8) | Déploiement manuel du frontend sur le Wyse à la demande de l'utilisateur : Caddyfile + docker-compose.yml modifiés (`/srv/site` remplace `/srv/test`), `frontend/dist` copié via scp dans `~/lutecium/deploy/site`, ancienne page de test supprimée (repo + serveur), Caddy recréé. Vérifié : `https://lutecium.app` sert le vrai squelette (confirmé par navigateur). Incident réseau transitoire vers le Wyse pendant l'opération (2e occurrence de la session), résolu tout seul en ~15s. Reprise du backend : passage à P1-02. |
| 2026-07-20 | S1 (suite 9) | P1-02 fait : backend/Dockerfile (python:3.12-slim + ffmpeg, user non-root) + .dockerignore + deploy/docker-compose.dev.yml. Pas de Docker sur la machine de dev → build et tests faits directement sur le Wyse (répertoire temporaire, image supprimée après coup, rien laissé en place) : /api/health OK, uid=1000 non-root confirmé, ffmpeg 7.1.5 OK. Passage à P1-03 (POST /api/analyze). |
| 2026-07-20 | S1 (suite 10) | P1-03 fait : POST /api/analyze (yt-dlp via API Python uniquement, S-05). Tests unitaires mockés + tests réels contre YouTube en local (vraie vidéo et vraie playlist). Bug réel trouvé et corrigé : ajout de `extract_flat=\"in_playlist\"` pour éviter que yt-dlp résolve toute une playlist avant de la rejeter (passé de 30s+ timeout à 2,5s). Prochaine étape : P1-04 (file FIFO persistée en BDD). |
| 2026-07-20 | S1 (suite 11) | P1-04 fait : SQLAlchemy 2.x async + aiosqlite, modèle Download, POST /api/downloads avec position FIFO, réconciliation au démarrage (P-03). 7 tests passent (DB mémoire pour les tests via conftest.py + StaticPool). Bug trouvé et corrigé au test réel : création manquante du répertoire `data/` avant ouverture du fichier SQLite. Suite : P1-05 (worker de téléchargement + fusion ffmpeg). |
| 2026-07-20 | S1 (suite 12) | P1-05 fait : worker (pool asyncio consommant une file en mémoire), bus d'événements thread-safe (events.py), run_download dans ytdlp.py (fusion ffmpeg automatique via yt-dlp, jamais de shell). Note : ASGITransport des tests httpx ne déclenche pas le lifespan → tests unitaires isolés du vrai worker, donc validation en réel indispensable : serveur uvicorn réellement lancé, vraie vidéo YouTube téléchargée et fusionnée (vérifié avec ffprobe : pistes h264+opus), taille exacte en base, et chemin d'échec vérifié (ID vidéo invalide → failed + message FR). Suite : P1-06 (modes audio seul / sous-titres). |
| 2026-07-20 | S1 (suite 13) | P1-06 fait : modes audio (FFmpegExtractAudio) et sous-titres (writesubtitles) ajoutés au worker, dispatch propre par mode dans ytdlp.py. Testé en réel : mp3 valide produit, .vtt anglais valide produit. Suite : P1-07 (assainissement du nom de fichier personnalisé, à câbler dans le worker). |
| 2026-07-20 | S1 (suite 14) | **Phase 1 terminée (16/16).** P1-07 à P1-15 développés ensemble (fortement couplés dans worker.py/ytdlp.py/downloads.py) puis validés en réel un par un : nom de fichier personnalisé, SSE (`curl -N` en direct : downloading→progress→done avec bytes/speed/eta réels), estimation du temps d'attente, annulation coopérative en cours de téléchargement (fichiers nettoyés), lien signé (fichier téléchargé et vérifié), TTL/purge (tests pytest dédiés), limites (file pleine → 429, cap disque → 507, fichier trop volumineux → message clair), erreurs FR, rate limiting (429 après 10 requêtes/min). Deux bugs réels trouvés et corrigés pendant les tests : message d'erreur trompeur pour les fichiers trop volumineux (yt-dlp échoue silencieusement), et un test pytest fragile (tampering du dernier caractère base64, bits « don't care »). P1-16 : critère de phase validé avec de vraies vidéos YouTube ET TikTok téléchargées de bout en bout (ffprobe confirme les codecs), 26 tests pytest passent. 1 commit (fonctionnalités fortement couplées, détail par tâche ici). Suite : synchronisation + déploiement sur le Wyse. |
| 2026-07-20 | S1 (suite 15) | Déploiement backend en production : `lutecium-api` intégré à deploy/docker-compose.yml (build depuis ../backend, volume nommé pour data/), Caddyfile mis à jour (reverse proxy /api/* vers lutecium-api:8000), SECRET_KEY de prod généré et posé uniquement sur le serveur. Backend synchronisé sur le Wyse (~/lutecium/backend), buildé et démarré. Vérifié en réel via le vrai domaine public : /api/health, analyse YouTube, création de job, téléchargement, fusion (statut done + file_url) — tout confirmé fonctionnel, aucun souci de permissions sur le volume Docker non-root. Le Wyse est devenu injoignable (ping/SSH/HTTPS, ~1 min, plus long que les micro-accrocs habituels de la session) juste avant la toute dernière vérification (récupération du fichier via le lien signé) → signalé à l'utilisateur, développement de la Phase 2 lancé en attendant plutôt que de bloquer. |
| 2026-07-20 | S1 (suite 16) | **Phase 2 terminée (9/9).** P2-01 : Alembic (schéma complet, modèles User/Session/GuestDownload/DailyUsage/Setting) + WAL, testé en réel. P2-02..04 : inscription, connexion, sessions, verrouillage brute-force — testés en réel (curl + cookie jar) ; 2 bugs trouvés et corrigés (cookie Secure non renvoyé par httpx sur http comme un vrai navigateur → `settings.secure_cookies` ; SQLite renvoie des datetimes naïfs même en `timezone=True` → comparaison Python cassée, corrigée avec `as_aware_utc`). P2-05 : mode invité (cookie+IP), bug de fond trouvé et corrigé (base de test en mémoire partagée entre fichiers pytest → nettoyage centralisé dans conftest.py). P2-06 : quota-cadeau conforme à la spec UI, testé en réel avec le message exact. P2-07 : reset admin testé (parcours HTTP complet). P2-08 : overrides runtime testés en réel (effet immédiat d'un changement admin sur l'API). P2-09 : critère de phase validé en réel de bout en bout (invité → 1 téléchargement → invitation → inscription → quota suivi correctement), 46 tests pytest passent. Phase 2 pas encore synchronisée/déployée sur le Wyse (en attente que le serveur soit de nouveau joignable). |
| 2026-07-21 | S2 | Wyse de nouveau joignable. Ajout de `GET /api/me/downloads` (historique paginé, documenté dans PLAN §3 mais jamais implémenté — nécessaire pour P3-10) : `site` dérivé du nom d'hôte de l'URL à la création du job, `url`/`options` renvoyés pour permettre le « retélécharger ». 3 tests ajoutés (49 passent). Synchronisation backend Phase 2 sur le Wyse (rsync, rsync installé sur le serveur au passage) : bug de déploiement trouvé et corrigé — la base SQLite de prod contenait encore la table `downloads` de la Phase 1 (`create_all`, avant Alembic), la migration initiale tentait de la recréer → conteneur en boucle de redémarrage. Ancienne base mise de côté (`lutecium.db.pre-phase2.bak`, une seule ligne de test perdue, aucune donnée réelle), migration rejouée proprement. Coupure réseau transitoire vers le Wyse pendant le diagnostic (même symptôme que les incidents précédents), résolue seule après ~1 min. **Vérifié en réel en production** : inscription + session (`/api/auth/register`, `/api/auth/me`), téléchargement complet bout en bout via un vrai lien signé (vidéo YouTube réelle, ffprobe confirme av1+opus, taille et nom de fichier corrects — dernière étape de la Phase 1 jamais confirmée jusqu'ici, désormais close), et `/api/me/downloads`. Backend Phase 1+2 entièrement synchronisé et stable sur le Wyse. Suite : implémentation du frontend Phase 3 (P3-03 à P3-14). |
| 2026-07-21 | S2 (suite) | **Phase 3 quasi terminée (13/14).** Frontend câblé sur le vrai backend : P3-02 (drawer nav + menu compte), P3-03..05 (flux A→F complet, SSE natif), P3-06/07 (gestionnaire de téléchargements découplé du bloc central + toasts + notifications navigateur), P3-08/09 (auth + reprise d'URL + parcours invité), P3-10/11 (historique, compte, quota-cadeau — a nécessité l'ajout de `GET /api/me/downloads` côté backend, voir plus haut), P3-12 (PWA : manifest, service worker minimal, share target Android testé en réel via `?url=`), P3-13 (accessibilité : cibles 44px, aria-live progression, `prefers-reduced-motion` via `MotionConfig`). Tout testé en réel dans le navigateur avec de vraies vidéos YouTube (pas de mock) : parcours invité complet, inscription avec reprise d'URL, téléchargement connecté avec historique et retélécharger, quota-cadeau simulé en base de dev. `tsc`/`build`/`lint` propres à chaque étape. 3 commits (P3-02..11, P3-12/13, cf. historique git pour le détail). Reste : **P3-14 (déploiement du build sur le Wyse)**, bloqué par une déconnexion du serveur en cours de session (confirmée par l'utilisateur) — build de production prêt localement, à synchroniser dès que le Wyse répond de nouveau. |
| 2026-07-21 | S2 (suite 2) | **Phase 4 terminée (7/7), critère de sortie validé.** Wyse resté injoignable (confirmé par l'utilisateur) → développement de la Phase 4 (backend + frontend, ne nécessite pas le Wyse) plutôt que d'attendre. Backend : gestion utilisateurs (A-10), métriques (A-11, `app/services/metrics.py`), état système via psutil (A-12, `/sys` monté ro pour la température, pas de socket Docker par choix de conception), journal (A-13) + rotation des logs applicatifs (M-02), actions rapides (A-14 : purge fichiers/vidage file/maj yt-dlp) + 2 flux SSE (`/admin/metrics/stream`, `/admin/system/stream`) pour le temps réel demandé par le CDC UI §7.3. 6 tests pytest ajoutés (55 passent). Frontend : dashboard `/admin` (sidebar 4 sections, garde de rôle), jauges/histogramme en SVG maison, table utilisateurs avec actions, journal filtrable. Constat : aucun mécanisme de création de compte admin n'existe encore (P5-02) → testé en promouvant un compte de test par SQL direct en dev uniquement. Tout vérifié en réel dans le navigateur (suspension → connexion refusée confirmée par l'API, suppression, jauges avec les vraies métriques CPU/RAM/température/disque de la machine de dev, SSE testé via `curl -N`). `tsc`/`build`/`lint` propres. 4 commits. Reste en attente pour les deux phases : **synchronisation/déploiement sur le Wyse** (P3-14, backend Phase 4 non plus). |
| 2026-07-21 | S2 (suite 3) | **Revue de l'implémentation** (Phases 0-4) à la demande de l'utilisateur. Suite pytest (59) + `tsc`/`build`/`lint` frontend relancés, propres. Relecture ciblée du code le plus récent/le moins couvert (Phase 4). 2 bugs réels trouvés et corrigés : (1) `app/core/system.py` mesurait toujours le disque racine (`Path.anchor` vaut `/` sous Linux) au lieu du volume réel des téléchargements — latent, sans conséquence sur le déploiement actuel à un seul volume, mais faux si un jour séparé ; (2) l'API `PATCH /api/admin/users/{id}` acceptait n'importe quelle valeur de `daily_quota_gb` sans validation — une saisie invalide côté frontend (`Number("abc")` → `NaN` → `JSON.stringify` → `null`) réinitialisait silencieusement le quota au défaut au lieu d'afficher une erreur ; ajout d'une contrainte `gt=0` + message d'erreur explicite côté frontend. 1 test ajouté. Constats mineurs non corrigés (faible sévérité, à traiter en Phase D/polish) : dashboard admin sans messages d'erreur réseau (page bloquée sur « Chargement… » en cas d'échec), `window.confirm`/`prompt` natifs plutôt que des modales stylées, une action « annuler » côté frontend peut échouer silencieusement (promesse non gérée), léger risque de décalage UTC/heure locale dans les métriques « aujourd'hui » (hérité du code existant, pas une régression de cette session), `list_users` fait N+1 requêtes (acceptable vu le nombre d'utilisateurs attendu sur un service auto-hébergé). Aucune régression trouvée dans le code des Phases 1-2 (non retouché depuis, vérifié par `git log`). |
| 2026-07-21 | S2 (suite 4) | **Identifiants admin réels reçus de l'utilisateur** (pseudo = email, mot de passe fourni en chat) : configurés dans `backend/.env` (gitignoré, jamais commité) pour le dev local via le bootstrap `ADMIN_PSEUDO`/`ADMIN_PASSWORD` (P5-02 partiel, S2 suite 2) — ancienne base de test locale (`testeur_flow` promu manuellement) archivée en `.old-testdata`, compte admin réel créé au démarrage et vérifié (login réel réussi). **À faire ce soir lors de la synchronisation prod** : ajouter les deux mêmes variables dans `deploy/.env` sur le Wyse (jamais dans git) pour que le vrai compte admin soit créé en production. Correction de tous les points d'implémentation restants de la revue précédente (décalage UTC/heure locale dans les métriques, N+1 requêtes `list_users`, absence de gestion d'erreur réseau dans les 4 pages admin, annulation de téléchargement pouvant échouer silencieusement) — voir commit. Robustesse de la mise à jour yt-dlp (M-01) renforcée à la demande explicite de l'utilisateur : horodatage de la dernière mise à jour réussie tracé en base et affiché sur `/admin/system` (alerte si > 36h, la fraîcheur devient vérifiable) ; `deploy/cron/update-ytdlp.sh` réécrit pour appeler l'action admin déjà testée plutôt que dupliquer sa logique — retries avec backoff, connexion en boucle locale forcée (indépendante du DNS/routeur), vérification post-redémarrage, fréquence recommandée relevée à toutes les 6h (garde-fou 409 rend ça sûr). Tout testé en réel en local (`tsc`/`build`/pytest propres, 59 tests). **À poser ce soir également** : la crontab `update-ytdlp.sh` sur le Wyse (commande documentée en tête du script). |
| 2026-07-24 | S3 | Reprise après 3 jours. **Constat** : le Wyse est injoignable depuis l'environnement de session (LAN `192.168.1.186` hors de portée, `https://lutecium.app`/`lutecium.duckdns.org` résolvent en DNS mais ne répondent pas sur 443 — cohérent avec la déconnexion notée en fin de S2). Décision utilisateur : travailler en local sur la Phase 5 (industrialisation) en attendant, synchronisation prod dès que le Wyse répond. T-03/P5-07 : README.md racine, doc `cookies.txt`, support technique manquant du fichier cookies.txt comblé au passage (voir Décisions). P5-04 : CSP ajoutée au Caddyfile, vérifiée sans violation en servant le build de production dans un vrai navigateur. P5-05 : durcissement `docker-compose.yml` (no-new-privileges partout, read_only+tmpfs sur `lutecium-api`) — non testable contre un vrai Docker ici, statut 🧪. P5-03 : script de backup BDD (`sqlite3.backup()` à chaud, cohérent en WAL), constat que DuckDNS/nettoyage n'ont pas besoin de cron dédié (déjà couverts). P5-02 : script de configuration interactive (`configure.sh`) — **2 bugs réels trouvés et corrigés pendant les tests** (relecture du `.env` via `source` cassée par une valeur contenant une virgule, boucle infinie possible sur EOF pendant la confirmation du mot de passe admin), testé en réel (premier lancement + relance idempotente, `SECRET_KEY`/mot de passe préservés). P5-06 : script « beau terminal » (zsh/starship/alias/motd), bloc `.zshrc` généré testé en réel (syntaxe + idempotence), installation zsh/starship elle-même non testable sans root sur une vraie machine. P5-01 : `install-lutecium.sh` orchestrant tout ce qui précède, logique de parsing/crontab testée isolément, bout-en-bout non testable sans machine Debian vierge. **Phase 5 : 3/8 ✅ + 4/8 🧪** (tout sauf P5-08, qui exige une vraie réinstallation chronométrée). 63 tests pytest backend toujours au vert (non retouchés cette session). Suite : Phase D (design, nécessite de clarifier l'outillage disponible) ou synchronisation prod dès que le Wyse répond. |
| 2026-07-24 | S3 (suite) | Utilisateur confirme : Wyse débranché volontairement (pas un incident), reconnexion prévue ce soir. Démarrage Phase D à la demande explicite de l'utilisateur : D-06 (nouvelle tâche, hors CDC) — mascotte 3D hamster fournie par l'utilisateur intégrée comme élément de décor déplaçable/tournable sur les pages publiques (voir Décisions pour le détail technique et le bug d'événements trouvé/corrigé). Nouvelles dépendances frontend : `three`, `@react-three/fiber`, `@react-three/drei`, `three-stdlib`. `tsc`/`lint`/`build` propres, testé en réel dans le navigateur (rotation par glissé, déplacement par poignée, adaptation aux deux thèmes, `aria-hidden` confirmé, CSP posée en P5-04 revérifiée sans violation avec cette nouvelle dépendance). D-01 à D-05 (design system, wordmark, maquettes, animations, contrastes) restent à faire — nécessitent de clarifier l'outillage avec l'utilisateur avant de s'y mettre. |
| 2026-07-24 | S3 (suite 2) | Utilisateur demande de continuer D-01 à D-05. **Phase D quasi close (5/6 ✅ + 1 🧪)**. D-05 (audit contrastes, fait en premier pour informer le reste) : **3 échecs WCAG AA réels trouvés et corrigés** dans `tokens.css` (texte secondaire clair, succès clair, bordures — voir Décisions), effet de bord sur la jauge quota-cadeau du menu compte corrigé avec un liseré plutôt qu'une nouvelle couleur. D-01 : monospace tranchée (**JetBrains Mono**, auto-hébergée via `@fontsource`), sans-serif confirmée système, `docs/design-system.md` créé comme référence versionnée. D-02 : piste `lutecium▌` confirmée retenue ; **comblé le manque flagué depuis P3-12** (icônes PWA jamais exportées en PNG) en rasterisant les SVG existants via un `<canvas>` navigateur (192/512 + maskable avec vraie zone de sécurité + apple-touch-icon dédié, l'ancien pointait vers un SVG peu fiable sur iOS). D-03 : périmètre adapté — les maquettes existent déjà (construites et testées en réel au fil de S2), vérification ciblée sur les changements du jour (aucune couleur codée en dur hors tokens, confirmé par grep exhaustif) plutôt qu'une nouvelle galerie de captures, faute d'avoir pu stabiliser l'outil de capture cette session (statut 🧪). D-04 : **vrai manque trouvé** — seul l'état E (`DoneCard`) avait une animation d'entrée, les transitions A→D étaient des remplacements instantanés sans morphing malgré la priorité n°1 du CDC ; corrigé avec `AnimatePresence` dans `MainFlow.tsx` (fondu+glissement sur chaque transition). Quota-cadeau : jauge désormais plafonnée à 130 % (au lieu de 100 %) pour réellement « dépasser élégamment » comme demandé, au lieu d'être plafonnée au bord du conteneur (raffinement de la déviation notée en P3-11). **Incident d'outillage notable** : le panneau navigateur s'est bloqué en cours de session sur les captures d'écran (timeout systématique, même sur une page externe neutre) — diagnostiqué comme une dégradation propre à un onglet réutilisé trop longtemps (confirmé en ouvrant un onglet neuf : rendu et animations immédiatement corrects, `requestAnimationFrame` de nouveau actif). `tsc`/lint/build propres tout du long. 5 commits (D-05, D-01, D-02, D-03 documentation seule, D-04). Reste : D-03 à repasser en ✅ avec une vraie galerie de captures si l'outil coopère une prochaine fois ; sinon Phase D est fonctionnellement complète. |
| 2026-07-24 | S3 (suite 3) | **Synchronisation prod complète, Wyse de nouveau joignable.** Backend (rsync), frontend (build du jour), `deploy/` (Caddyfile CSP, docker-compose durci, cron/, provision/) synchronisés — le Wyse servait encore le squelette du 20 juillet. **Bug réel rencontré et corrigé en cours de déploiement** : `docker compose build` fait sans `up -d` → décalage temporaire frontend neuf/backend ancien → 422 sur `/api/analyze` en prod (signalé par l'utilisateur en direct), corrigé en terminant le déploiement. **Cause racine des coupures réseau du projet identifiée et corrigée** : Debian installé avec bureau GNOME complet (pas de profil serveur choisi à l'installation) → mise en veille automatique après inactivité, expliquant les « incidents transitoires » récurrents depuis S1. Corrigé : cibles de veille masquées, cible par défaut `multi-user.target`, GDM désactivé — vérifié en réel après un redémarrage complet (boot headless, conteneurs relevés seuls). Mise à jour système appliquée au passage. Compte admin réel configuré en prod (identifiants transmis en clair sur autorisation explicite de l'utilisateur) — **bug réel rencontré** : `docker compose restart` ne relit pas `.env`, il a fallu `up -d` pour que le bootstrap admin s'applique ; login vérifié (200). Crontab posée (yt-dlp 6h, backup BDD 3h) ; `backup-db.sh` testé en réel en production pour la première fois (backup à chaud pendant que l'app tournait, décompressé et relu avec succès). **Leçon d'outillage** : `read_page` (arbre d'accessibilité) peut renvoyer un instantané périmé sur un onglet réutilisé plusieurs fois dans la même session — une requête DOM directe (`document.querySelector`) a révélé que la carte d'aperçu s'affichait en fait correctement, alors que `read_page` montrait encore l'état de repos ; à privilégier pour toute vérification post-interaction JS. Parcours complet reconfirmé en réel dans un vrai navigateur (collage d'URL → analyse auto → aperçu avec titre/miniature/durée/poids corrects, aucune erreur console). P3-14, P5-03, P5-05 passés à ✅. |
